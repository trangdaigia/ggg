def localProperties = new Properties()
def localPropertiesFile = rootProject.file('local.properties')
if (localPropertiesFile.exists()) {
    localPropertiesFile.withReader('UTF-8') { reader ->
        localProperties.load(reader)
    }
}

def flutterRoot = localProperties.getProperty('flutter.sdk')
if (flutterRoot == null) {
    throw new GradleException("Flutter SDK not found. Define location with flutter.sdk in the local.properties file.")
}

def flutterVersionCode = localProperties.getProperty('flutter.versionCode')

def flutterVersionName = localProperties.getProperty('flutter.versionName')

apply plugin: 'com.android.application'
apply plugin: 'kotlin-android'
apply from: "$flutterRoot/packages/flutter_tools/gradle/flutter.gradle"
apply plugin: 'com.google.gms.google-services'  // Google Services plugin

def keystoreProperties = new Properties()
def keystorePropertiesFile = rootProject.file('key.properties')
if (keystorePropertiesFile.exists()) {
    keystoreProperties.load(new FileInputStream(keystorePropertiesFile))
}

android {

    // ----- BEGIN flavorDimensions (autogenerated by flutter_flavorizr) -----
    flavorDimensions "flavor-type"

    productFlavors {
        sod_vendor {
            dimension "flavor-type"
            // applicationId "sod_vendor.di4l.vn"
            applicationId "sod_vendorv2.di4l.vn"
            resValue "string", "app_name", "SOD Vendor"
            resValue "string", "google_map_key", "AIzaSyDNz8CSNl9f4zV9osSMBhp93BmxQeylE3w"

        }
        sob_express_vendor {
            dimension "flavor-type"
            applicationId "sobvendor.express.vn"
            resValue "string", "app_name", "GoEco Cửa Hàng"
            resValue "string", "google_map_key", "abc"
        }
        suc365_vendor {
            dimension "flavor-type"
            applicationId "suc365vendor.di4l.vn"
            resValue "string", "app_name", "SUC365 Vendor"
            resValue "string", "google_map_key", "AIzaSyDNz8CSNl9f4zV9osSMBhp93BmxQeylE3w"
        }
        g47_vendor {
            dimension "flavor-type"
            // applicationId "g47vendor.di4l.vn"
            applicationId "g47cuahang.di4l.vn"
            resValue "string", "app_name", "G47 Cửa Hàng"
            resValue "string", "google_map_key", "AIzaSyDNz8CSNl9f4zV9osSMBhp93BmxQeylE3w"
        }
        appvietsob_vendor {
            dimension "flavor-type"
            // applicationId "vietapp.vendor.di4l.vn"
            applicationId "vietapp.vendorv2.di4l.vn"
            resValue "string", "app_name", "AV Cửa Hàng"
            resValue "string", "google_map_key", "abc"
        }
        fasthub_vendor {
            dimension "flavor-type"
            applicationId "fasthub_vendor.di4l.vn"
            resValue "string", "app_name", "Fasthub Vendor"
            resValue "string", "google_map_key", "AIzaSyDNz8CSNl9f4zV9osSMBhp93BmxQeylE3w"
        }
        vasone_vendor {
            dimension "flavor-type"
            applicationId "vasone_vendor.di4l.vn"
            resValue "string", "app_name", "Vasone Vendor"
            resValue "string", "google_map_key", "aaaa"

        }
        goingship_vendor {
            dimension "flavor-type"
            // applicationId "vendor.goingship.vn"
            // applicationId "vendor.goingshipv2.vn"
            applicationId "gogoshipcuahang.di4l.vn"
            resValue "string", "app_name", "GoGoShip Cửa Hàng"
            resValue "string", "google_map_key", "aaaa"
        }
        grabxanh_vendor {
            dimension "flavor-type"
            // applicationId "goxanhcuahang.shipxanh247.com"
            //applicationId "vendor.ecoship247.com"
            applicationId "cuahang.ecoship247.com"
            resValue "string", "app_name", "EcoSHIP Cửa Hàng"
            resValue "string", "google_map_key", "aaaa"

        }
    }

    // ----- END flavorDimensions (autogenerated by flutter_flavorizr) -----


      compileSdkVersion 34

    

    sourceSets {
        main.java.srcDirs += 'src/main/kotlin'
    }

    lintOptions {
        disable 'InvalidPackage'
    }

    defaultConfig {
        // TODO: Specify your own unique Application ID (https://developer.android.com/studio/build/application-id.html).
        applicationId "sod_vendor.di4l.vn"
        minSdkVersion 23 //Required for Jitsi
        targetSdkVersion 34
        versionCode flutterVersionCode.toInteger()
        versionName flutterVersionName
        multiDexEnabled true
    }

    signingConfigs {
        sod_vendor {
            keyAlias keystoreProperties['keyAlias']
            keyPassword keystoreProperties['keyPassword']
            storeFile keystoreProperties['storeFile'] ? file(keystoreProperties['storeFile']) : null
            storePassword keystoreProperties['storePassword']
        }

        sob_express_vendor {
            keyAlias keystoreProperties['keyAlias']
            keyPassword keystoreProperties['keyPassword']
            storeFile keystoreProperties['storeFile'] ? file(keystoreProperties['storeFile']) : null
            storePassword keystoreProperties['storePassword']
        }

        suc365_vendor {
            keyAlias keystoreProperties['keyAliasSuc365']
            keyPassword keystoreProperties['keyPasswordSuc365']
            storeFile keystoreProperties['storeFileSuc365'] ? file(keystoreProperties['storeFileSuc365']) : null
            storePassword keystoreProperties['storePasswordSuc365']
        }

        g47_vendor {
            keyAlias keystoreProperties['keyAlias']
            keyPassword keystoreProperties['keyPassword']
            storeFile keystoreProperties['storeFile'] ? file(keystoreProperties['storeFile']) : null
            storePassword keystoreProperties['storePassword']
        }

        appvietsob_vendor {
            keyAlias keystoreProperties['keyAliasAppViet']
            keyPassword keystoreProperties['keyPasswordAppViet']
            storeFile keystoreProperties['storeFileAppViet'] ? file(keystoreProperties['storeFileAppViet']) : null
            storePassword keystoreProperties['storePasswordAppViet']
        }

       fasthub_vendor {
            keyAlias keystoreProperties['keyAlias']
            keyPassword keystoreProperties['keyPassword']
            storeFile keystoreProperties['storeFile'] ? file(keystoreProperties['storeFile']) : null
            storePassword keystoreProperties['storePassword']
        }

        vasone_vendor {
            keyAlias keystoreProperties['keyAlias']
            keyPassword keystoreProperties['keyPassword']
            storeFile keystoreProperties['storeFile'] ? file(keystoreProperties['storeFile']) : null
            storePassword keystoreProperties['storePassword']
        }

        grabxanh_vendor {
            keyAlias keystoreProperties['keyAliasShipXanh']
            keyPassword keystoreProperties['keyPasswordShipXanh']
            storeFile keystoreProperties['storeFileShipXanh'] ? file(keystoreProperties['storeFileShipXanh']) : null
            storePassword keystoreProperties['storePasswordShipXanh']
        }

        goingship_vendor {
            keyAlias keystoreProperties['keyAliasGoingShip']
            keyPassword keystoreProperties['keyPasswordGoingShip']
            storeFile keystoreProperties['storeFileGoingShip'] ? file(keystoreProperties['storeFileGoingShip']) : null
            storePassword keystoreProperties['storePasswordGoingShip']
        }

        release {
            keyAlias keystoreProperties['keyAliasAppViet']
            keyPassword keystoreProperties['keyPasswordAppViet']
            storeFile keystoreProperties['storeFileAppViet'] ? file(keystoreProperties['storeFileAppViet']) : null
            storePassword keystoreProperties['storePasswordAppViet']
        }
    }

    buildTypes {
      debug {
            // Use the signing config for each flavor
            signingConfig signingConfigs.debug
        }
        release {
            // TODO: Add your own signing config for the release build.
            // Signing with the debug keys for now, so `flutter run --release` works.
            productFlavors.sod_vendor.signingConfig signingConfigs.sod_vendor
            productFlavors.sob_express_vendor.signingConfig signingConfigs.sob_express_vendor
            productFlavors.suc365_vendor.signingConfig signingConfigs.suc365_vendor
            productFlavors.g47_vendor.signingConfig signingConfigs.g47_vendor
            productFlavors.appvietsob_vendor.signingConfig signingConfigs.appvietsob_vendor
            productFlavors.vasone_vendor.signingConfig signingConfigs.vasone_vendor
            productFlavors.fasthub_vendor.signingConfig signingConfigs.fasthub_vendor
            productFlavors.grabxanh_vendor.signingConfig signingConfigs.grabxanh_vendor
            productFlavors.goingship_vendor.signingConfig signingConfigs.goingship_vendor
            // signingConfig signingConfigs.release
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
}

flutter {
    source '../..'
}

dependencies {
    implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk7:$kotlin_version"
    implementation "com.google.firebase:firebase-messaging-ktx:21.0.1"
    implementation 'com.google.firebase:firebase-auth-ktx:20.0.4'
    implementation 'com.android.support:multidex:2.0.1'
    implementation 'com.google.guava:guava:29.0-jre'
    implementation "androidx.browser:browser:1.3.0"
}